# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Email Configuration (Resend)
RESEND_API_KEY=your-resend-api-key
LEAD_NOTIFY_FROM=info@yourdomain.com
LEAD_NOTIFY_TO=email1@yourdomain.com,email2@yourdomain.com

# API Configuration
NEXT_PUBLIC_API_BASE_URL=http://localhost:5001

# Deployment Tracking (Local Git Hooks Only)
PVS_WEBSITE_ID=your-website-uuid-here
PVS_API_URL=https://your-api-domain.com
PVS_BASE_URL=https://www.yourdomain.com

# Analytics - Mixpanel
NEXT_PUBLIC_MIXPANEL_TOKEN=your-mixpanel-project-token

# Error Tracking - Sentry
NEXT_PUBLIC_SENTRY_DSN=your-sentry-dsn-here

# Calendly - Contact page "Schedule a Strategy Call" embed
# Theme colors (light/dark) are injected automatically at runtime — do not add color params here
NEXT_PUBLIC_CALENDLY_URL=https://calendly.com/pixelversestudios-info/discovery

# Environment - set to "staging" on staging deployments to enable dev-only UI (e.g. layout pickers)
# Leave unset on production
NEXT_PUBLIC_APP_ENV=staging

# Google Places API - used server-side to fetch live rating + review count for homepage badge
# Enable "Places API (New)" in Google Cloud Console, restrict key to this API only
# Place ID: ChIJP9TTk-nyGIgRJLhBiKpq0Nw (PixelVerse Studios)
GOOGLE_PLACES_API_KEY=your-google-places-api-key
