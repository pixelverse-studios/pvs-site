import { Boxes, Layers, Rocket, Sparkles } from 'lucide-react';

import { Button } from '@/components/ui/button';
import { Container } from '@/components/ui/container';
import {
  Card,
  CardContent,
  CardDescription,
  CardFooter,
  CardHeader,
  CardTitle
} from '@/components/ui/card';
import { SectionHeader } from '@/components/ui/section-header';

const websitePackages = [
  {
    icon: Boxes,
    title: 'Core Lite',
    description: 'Get online quickly and affordably.'
  },
  {
    icon: Layers,
    title: 'Core Starter',
    description: 'Build a professional presence that lasts.'
  },
  {
    icon: Rocket,
    title: 'Core Growth',
    description: 'Scale with advanced features and flexibility.'
  },
  {
    icon: Sparkles,
    title: 'Core Premium',
    description: 'Unlock custom solutions for maximum growth.'
  }
];

export function WebsitePackagesSection() {
  return (
    <section className="py-16 md:py-24">
      <Container className="space-y-10">
        <SectionHeader
          align="center"
          eyebrow="Website Packages"
          title="Website Packages Snapshot"
          description="Choose a starting point built around performance, usability, and your growth goals."
          className="mx-auto max-w-3xl"
        />
        <div className="flex gap-6 overflow-x-auto pb-3 md:grid md:grid-cols-2 md:gap-6 md:overflow-visible">
          {websitePackages.map(({ icon: Icon, title, description }) => (
            <Card
              key={title}
              className="group flex min-w-[260px] flex-col border border-[var(--pv-border)] bg-[var(--pv-bg)]/95 transition-all duration-300 hover:-translate-y-1 hover:border-[var(--pv-primary)] hover:shadow-[0_26px_60px_-40px_rgba(63,0,233,0.75)] dark:bg-[var(--pv-surface)]/95 md:min-w-0"
            >
              <CardHeader className="flex flex-row items-start gap-4">
                <span className="inline-flex h-12 w-12 items-center justify-center rounded-full bg-[linear-gradient(135deg,var(--pv-primary),var(--pv-primary-2))] text-white shadow-[0_22px_46px_-34px_rgba(63,0,233,0.85)] transition-transform duration-300 group-hover:-translate-y-1">
                  <Icon className="h-6 w-6" aria-hidden="true" />
                </span>
                <div>
                  <CardTitle>{title}</CardTitle>
                  <CardDescription>{description}</CardDescription>
                </div>
              </CardHeader>
              <CardContent className="flex-1 text-sm text-[var(--pv-text-muted)]">
                Our fully coded builds skip templates and prioritize UX, ensuring your site performs
                beautifully from launch day and keeps evolving with your needs.
              </CardContent>
              <CardFooter>
                <Button variant="secondary" size="sm" className="w-full">
                  Learn More
                </Button>
              </CardFooter>
            </Card>
          ))}
        </div>
      </Container>
    </section>
  );
}
