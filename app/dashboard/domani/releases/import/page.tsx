import { MarkdownImportPage } from '../components/markdown-import-page';

export const metadata = {
  title: 'Import Domani Release Markdown | Dashboard',
  robots: { index: false, follow: false },
};

export default function ImportReleaseMarkdownPage() {
  return <MarkdownImportPage />;
}
